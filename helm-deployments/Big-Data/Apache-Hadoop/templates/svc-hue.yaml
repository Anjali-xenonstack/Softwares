apiVersion: v1
kind: Service
metadata:
  name: {{ template "bigdata.fullname" . }}-hue
spec:
  type: NodePort
  ports:
  - port: 8888
    name: httphue
  selector:
    app: {{ template "bigdata.name" . }}-hue
