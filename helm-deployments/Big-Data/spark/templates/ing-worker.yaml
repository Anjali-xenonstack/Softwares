{{- if .Values.Worker.ingress.enabled -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "spark.fullname" . }}
  labels:
    heritage: {{ .Release.Service | quote }}
    release: {{ .Release.Name | quote }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
  annotations:
    {{- range $key, $value := .Values.Worker.ingress.annotations }}
      {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  rules:
  - host: {{ .Release.Name }}.{{ .Values.Worker.ingress.domainName}}
    http:
      paths:
      - path: /
        backend:
          serviceName: {{ template "spark.fullname" . }}-worker
          servicePort: 8081
{{- end -}}
