apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-pig
  labels:
    app: {{ template "hdp.name" . }}-pig
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  strategy:
    rollingUpdate:
      maxUnavailable: 0
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ template "hdp.name" . }}-pig
        release: {{ .Release.Name }}
    spec:
      terminationGracePeriodSeconds: 0
      containers:
        - name: pig
          image: 190742198407.dkr.ecr.us-west-2.amazonaws.com/xl/hdp:pigv1.2 #1.1
          imagePullPolicy: "IfNotPresent"
          command: [ "/bin/bash", "-c", "--" ]
          args: [ "/etc/bootstrap.sh -bash; while true; do sleep 30; done;" ]
          env:
            - name: HDFS_IP
              value: {{ template "hdp.fullname" . }}-hdfs-nn
