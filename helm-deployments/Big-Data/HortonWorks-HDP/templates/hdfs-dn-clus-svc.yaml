# A headless service to create DNS records
apiVersion: v1
kind: Service
metadata:
  name: {{ template "hdp.fullname" . }}-hdfs-dn-dnn
  labels:
    app: {{ template "hdp.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    component: hdfs-dn
spec:
  ports:
  - name: dfs
    port: 9000
    protocol: TCP
  - name: dnhdfs
    port: 50075
  type: NodePort
  selector:
    app: {{ template "hdp.name" . }}
    release: {{ .Release.Name }}
    component: hdfs-dn
