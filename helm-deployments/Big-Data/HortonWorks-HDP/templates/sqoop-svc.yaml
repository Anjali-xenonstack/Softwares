apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-sqoop
  labels:
    app: {{ template "hdp.name" . }}-sqoop
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: NodePort
  ports:
    - name: datasqoop
      port: 12000
    - name: node
      port: 9000
  selector:
    app: {{ template "hdp.name" . }}-sqoop
    release: {{ .Release.Name }}
