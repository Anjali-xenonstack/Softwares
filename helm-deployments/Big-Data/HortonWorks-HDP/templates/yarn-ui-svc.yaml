# Service to access the yarn web ui
apiVersion: v1
kind: Service
metadata:
  name: {{ template "hdp.fullname" . }}-yarn-ui
  labels:
    app: {{ template "hdp.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    component: yarn-ui
spec:
  ports:
  - port: 8088
    name: yarnweb
  type: LoadBalancer
  selector:
    app: {{ template "hdp.name" . }}
    component: yarn-rm
